import React, {useState} from 'react';
import {
  StyleSheet,
  View,
  SafeAreaView,
  Image,
  TextInput,
  Button,
  TouchableHighlight,
  Text,
  TouchableOpacity,
  TouchableWithoutFeedback,
  ScrollView,
  StatusBar,
} from 'react-native';

const App = () => {
  const [state, setState] = useState('123');
  const confirm = () => {
    console.log(state);
  };
  return (
    <SafeAreaView style={styles.viewContainer}>
      <ScrollView showsVerticalScrollIndicator={false}>
        <Text>
          React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不
          React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第
          React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100% 的宽度，以确保它拉伸它的全宽，然后将
          React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100%
          的宽度，以确保它拉伸它的全宽，然后将二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100%
          的宽度，以确保它拉伸它的全宽，然后将正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100% 的宽度，以确保它拉伸它的全宽，然后将
          texalign 设置为正确 ...React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100% 的宽度，以确保它拉伸它的全宽，然后将
          texalign 设置为正确 ...React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100% 的宽度，以确保它拉伸它的全宽，然后将
          texalign 设置为正确 ...React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100% 的宽度，以确保它拉伸它的全宽，然后将
          texalign 设置为正确 ...React-native：textAlign：'right'样式不正确 -
          React-native：textAlign：'right'样式不正确. 我对 react-native
          css-styling 很陌生。. 并且有以下代码：.
          问题是第二个“textAlign：'right'”不起作用 - 文本仍在左侧。.
          我希望文本在同一行，但（显然）右边的第二个文本对象。. 有什么提示吗？.
          谢谢！. 为您的文本标签提供 100% 的宽度，以确保它拉伸它的全宽，然后将
          texalign 设置为正确 ...
        </Text>
      </ScrollView>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'center',
    marginHorizontal: 10,
  },
});

export default App;
